(ns documentation.hara-sort)

"[hara.sort](https://github.com/zcaudate/hara/blob/master/src/hara/sort.clj) contain functions and utilities for sorting algorithms."


[[:chapter {:title "sort.hierarchical" :link "hara.sort.hierarchical"}]]

"Add to `project.clj` dependencies:

    [im.chit/hara.sort.hierarchical \"{{PROJECT.version}}\"]

**hara.sort.hierarchical** gives a list of dependents for each key"

[[:api {:namespace "hara.sort.hierarchical"  :title ""}]]

[[:chapter {:title "sort.topological" :link "hara.sort.topological"}]]

"Add to `project.clj` dependencies:

    [im.chit/hara.sort.topological \"{{PROJECT.version}}\"]

**hara.sort.topological** gives an order of preference for a set of hierarchies"

[[:api {:namespace "hara.sort.topological" :title ""}]]
